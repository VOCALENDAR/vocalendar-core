 * タグの名前が直接変更された時に、何らかの方法で書き出し先を更新する
   * タグ名変更不能にしておいたほうがいいか？
 * カレンダーの追加/削除タグが変更された時に強制反映するしくみ
 * import でやってるので同じイベントの更新がちゃんといってない？ 要確認
 * sanity check
   * empty title
   * non-word char in tag name 
   * タグを外してもタイトルに括弧をもっているもの
 * layout 外すモードで UI の出力
   * 詳細画面
 * アクションヒストリの記録を各種コントローラに追加
 * タグにシステム属性フラグを追加?
   * 「メイン」「新曲はっぴょう予定」などだが、国際化をどうするか……
 * イベントモデルのURI属性を整理
 * イベントの編集 UI を直す (画像つけられるように)
 * 詳細表示画面の出力 (レイアウト無し)
 * こちらで作られたイベントインスタンスを google に書き込んだ時に、
   各種 google 固有データを書き戻す?
   * こちら固有じゃなくてもつきあわせて確認した方が良いか？
   * 書き出し先の ID と元の ID は違うのか？
   * 関連テーブルを用意して、どういう風に書き出したか全ての情報を
     とっておく？　流石にいらんか。
 * 動的にユーザ作った時に名前とか入れるように users/edit に飛ばす
   * users/edit のビューを整備
   * user の他のビューも整備
 * google oauth 動的作成の時 editor ロールをふる判定を
   calendar 側の所有ユーザからちぇっくするように変更する
 * bootstrap を gem のやつにいこう
 * カレンダーのオーナの選択を管理者だけに絞る
 * editor のチェックを管理者側からやるように
   * カレンダー側にフラグの追加
 * 申請フォームを取り込む
   * 新曲
   * イベント登録申請
 * アビリティの更新(カレンダーをユーザ変更不可に)
 * an_event.status の通常更新不可に。accsssible を絞る :as を活用すること
 * イベントのタイムゾーンが書き出し時にgoogle側に反映されているか確認する
 * 取り込みの時に html_link から eid を取り出して保存しておく
 * term フォーマッタで allday　の時に終了日がおかしい -1.day しないといけない。テストもなおすこと。
 * テストで admin がリダイレクトされるようになったのに対応してない
 * カレンダーの同期時に何らかの手段でロックする
 * P-cal の入力フォームと要素の取り込み(フィールド付きタグで保存する仕組み)
